{
  "name": "part4",
  "version": "1.0.0",
  "description": "Blog list application",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "watch": "cross-env NODE_ENV=test jest --watch --runInBand",
    "userTest": "cross-env NODE_ENV=test jest --verbose --runInBand -- tests/user_api.test.js",
    "blogTest": "cross-env NODE_ENV=test jest --verbose --runInBand -- tests/blog_api.test.js",
    "start:test": "cross-env NODE_ENV=test node index.js"
  },
  "keywords": [
    "Blog"
  ],
  "author": "Infandel",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.3.1",
    "nodemon": "^2.0.14",
    "supertest": "^6.1.6"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.12",
    "mongoose-unique-validator": "^2.0.1"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
